---

env_vars_env_name: "{{ env_name }}"  # to be set by playbook
env_vars_directory: "{{ playbook_dir }}/config"
env_vars_common_file: "{{ env_vars_directory }}/default.yaml"
env_vars_env_file: "{{ env_vars_directory }}/{{ env_vars_env_name }}.yaml"
